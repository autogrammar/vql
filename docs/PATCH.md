# PATCH

## Purpose

PATCH command in the VQL Visual Query Language DSL.

## Syntax

See `packages/dsl2vql/src/dsl2vql/schema/commands/patch.schema.json` for the canonical JSON Schema contract.

## Inputs

Defined by the command schema.

## Outputs

Defined by the command schema.

## Errors

See `docs/ERROR/` for error codes.

## Examples

See `docs/app.vql.json` and `examples/` directory.
